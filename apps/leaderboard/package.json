{
  "//": "Leaderboard service: idempotent ELO apply + rank/top-N reads (Redis sorted set). Phase 0 = health only. Hand-written.",
  "name": "@chess/leaderboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@chess/config": "workspace:*",
    "@chess/telemetry": "workspace:*",
    "fastify": "^5.2.0"
  }
}
